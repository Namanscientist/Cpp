// TERNARY OPERATOR.
// With ternary operator we pply if-else in same line.
#include<iostream>
using namespace std;
int main(){
    int n;
    cout<<"Enter the number : ";
    cin>>n;
    (n%2==0) ? cout<<"Even number" : cout<<"Odd number";
}